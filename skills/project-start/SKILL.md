---
name: project-start
title: "Project Start: старт проекта по Golden Path NeuroLab"
description: Точка входа в любой новый проект NeuroLab. Сначала выясняет уровень подачи — разработчик перед ним или человек без кода — и дальше объясняет всё соответственно. Проводит короткое Discovery (гипотеза результата, границы задачи, имя проекта) — детальные требования снимает /nlab:prototype на макете, фиксирует его в intent.md, разворачивает обязательный каркас — AGENTS.md и CLAUDE.md в корне (в CLAUDE.md — ежедневная проверка актуальности скиллов готовой командой) и папку project-docs/ с NOTES.md, EVIDENCE.md, feature-list.json, закрепляет дефолтный стек компании (React из темплейта, pydantic-ai, LLM только через OpenAI-совместимый API) и проверяет доступы — выданный git-репозиторий и ключ LLM. Собирает PLAN.html — разбор проекта простым языком для не-инженера. Вызывается командой /nlab:project-start. Использовать, когда пользователь начинает новый проект, говорит «начать проект», «с чего начать», «новый сервис/агент/интерфейс» или просит сразу писать код без Discovery.
owner: alexgl-dev
version: 4.2.2
status: in-use
scope: любой новый проект NeuroLab (сервис, агент, мультиагентная система, фронтенд) — до выбора стека и первой строки кода
stage: discovery
depends_on: []
autonomy_level: R4
last_reviewed: 2026-09-07
registry_url: https://github.com/vibe-nlab/nlab-vibeskills
update_check: per_session
---

# Project Start: старт проекта по Golden Path NeuroLab

## 1. Назначение (Purpose)

Превращает свободный запрос пользователя («хочу сервис, который…») в
зафиксированное намерение и готовый к работе каркас проекта: `intent.md` с
проверяемыми критериями приёмки, обязательные структурные файлы,
дефолтный стек компании и подтверждённые доступы. Это первый скилл Golden Path —
`Discovery → Design → Prep → Deploy`; без его выхода остальные скиллы
работать не должны.

Скилл — исполняемая форма двух документов реестра:
[`AGENTS.md`](assets/AGENTS.md) (процесс) и
[`project_setup_best_practices.md`](references/project_setup_best_practices.md)
(стек и модели). **Читай их с диска в начале работы, не пересказывай по
памяти** — источник правил один, копий в скилле нет.

Все пути ниже — относительно каталога этого скилла:

```
SKILL_DIR = ${CLAUDE_PLUGIN_ROOT}/skills/project-start
├── assets/AGENTS.md                              # копируется в проект и адаптируется
├── assets/CLAUDE.md                              # → CLAUDE.md в корне: точка входа сессии
│                                                 #   + ежедневная проверка версии скиллов
├── assets/plan.template.html                     # → project-docs/PLAN.html, генерируется
├── assets/frontend-next-14-starter/              # копируется в <проект>/frontend
└── references/project_setup_best_practices.md    # только чтение, в проект не копируется
```

Служебные артефакты процесса живут в одной папке — `project-docs/`, чтобы
корень проекта оставался кодом, а не документами:

```
проект/
├── AGENTS.md                 # исключение — остаётся в корне, иначе агенты
│                             # не подхватят правила автоматически
├── CLAUDE.md                 # второе исключение — Claude Code читает его сам
│                             # в начале сессии: отсылка к AGENTS.md и проверка
│                             # актуальности скиллов раз в сутки
├── project-docs/
│   ├── intent.md
│   ├── NOTES.md
│   ├── EVIDENCE.md
│   ├── feature-list.json
│   ├── MOCK.html             # создаёт /nlab:prototype
│   └── arch/                 # создаёт /nlab:code-design
├── backend/  frontend/
└── .gitignore  .env.example
```

Дальше в тексте короткие имена (`intent.md`, `NOTES.md`) означают файлы из
`project-docs/`. В командах путь всегда указан полностью.

## 2. Входные артефакты (Inputs) — ОБЯЗАТЕЛЬНО

Скилл — точка входа, поэтому входов от других скиллов у него нет. Но есть
входы **от человека**, без которых работа не начинается: их нельзя
«додумать», их нужно получить в диалоге и явно зафиксировать.

| Артефакт | Обязателен? | Кто предоставляет | Что делать, если отсутствует |
|---|---|---|---|
| Запрос пользователя (свободная форма) | да | пользователь | нечего фиксировать — спросить, что за проект |
| Грубая гипотеза результата: что считать успехом в целом | да | пользователь | одной фразы достаточно — **детальные критерии снимает `/nlab:prototype` на макете**, здесь их не выпытывать. Совсем ничего не сказано — спросить «как поймём, что это вообще работает?» |
| Явные границы задачи (что НЕ входит) | да | пользователь | спросить прямо; пустой раздел в `intent.md` недопустим |
| Уровень пользователя: разработчик или не-инженер | да | пользователь | спросить **первым вопросом**, до всего остального: от этого зависит, как объясняется каждый следующий шаг. Не угадывать по формулировкам — спросить прямо. Записывается в проектный `AGENTS.md` и действует на все этапы |
| Название проекта (латиницей, kebab-case) | да | пользователь | спросить **на Discovery, не позже**: по имени выдают репозиторий и от него идут имена модулей. Рабочее имя папки (`TestAgent`, `new-project`) названием не считается — предложить вариант по смыслу и получить подтверждение |
| URL выданного git-репозитория (`sber-nlab/<проект>`) | да — **только к моменту первого push** | заявка `onboard <проект>` в `vibe-nlab/onboarding` (project_setup_best_practices.md п.5); пользователь должен быть в org `vibe-nlab` — инвайт дают ответственные ([@sanchezgl](https://t.me/sanchezgl) / [@KirillBorovkov](https://t.me/KirillBorovkov)) | локальную работу продолжать, push **не делать**, свой репозиторий **не создавать**; подать заявку `gh issue create -R vibe-nlab/onboarding -t "onboard <проект>" -b ""` и дождаться ответа в issue; пользователь не в org → попросить его запросить инвайт, назвав GitHub-никнейм |
| Ключ LLM (OpenRouter или гейт nlab) | да — только если проект вызывает LLM | пользователь / ответственные | каркас доделать, но не писать LLM-код на «заглушках-обходах»; остановиться и попросить запросить ключ |
| `assets/AGENTS.md` и `references/project_setup_best_practices.md` из `$SKILL_DIR` | да | плагин `nlab` | если файлы недоступны — работать по локальной копии в реестре, отметить в `EVIDENCE.md`, что источник правил не подтверждён |

## 3. Выходные артефакты (Outputs) — ОБЯЗАТЕЛЬНО

| Артефакт | Обязателен? | Формат | Кто читает дальше |
|---|---|---|---|
| `project-docs/intent.md` | да | markdown: гипотеза, границы, предположения. **Критерии приёмки — нумерованным списком** (`1.`, `2.`) в разделе `## Критерии приёмки`: в этом формате их читает проверка покрытия на Design | Design, Prep, ревьюер-человек |
| `project-docs/PLAN.html` | да | человекочитаемое объяснение проекта: суть, что делает, как работает, что на выходе, задачи, что нужно от пользователя. **Генерируется** из `intent.md` и `feature-list.json`, руками не правится; `/nlab:code-design` пересобирает | пользователь, приёмка, заказчик |
| `AGENTS.md` (**в корне проекта**) | да | копия правил реестра + проектная специфика, включая раздел «Уровень подачи» с ответом пользователя. Первой строкой — штамп `<!-- nlab-rules: v<версия плагина>, скопировано <YYYY-MM-DD> -->`: по нему видно, из какой версии сделана копия | любой агент/сотрудник, открывший проект |
| `CLAUDE.md` (**в корне проекта**) | да | копия `$SKILL_DIR/assets/CLAUDE.md`: имя и одна строка о проекте, отсылка к `AGENTS.md`, ритуал ежедневной проверки актуальности скиллов с готовой командой. Тот же штамп `nlab-rules` в первой строке. Claude Code читает этот файл сам в начале каждой сессии — поэтому проверка живёт здесь, а не в `AGENTS.md` | Claude Code в начале каждой сессии |
| `project-docs/NOTES.md` | да | append-only журнал: дата, этап, решение, обоснование | все последующие скиллы |
| `project-docs/feature-list.json` | да | **массив верхнего уровня** (не объект!), элемент — `{id, title, acceptance, status, depends_on}`; `id` в kebab-case и уникален | Design/Prep, декомпозиция на субагентов |
| `project-docs/EVIDENCE.md` | да | append-only: что проверено и как (проверяемые формулировки) | Prep, Deploy (без него Deploy не стартует) |
| `.gitignore` | да | содержит `.env`, `.env.*` (кроме `*.example`), `servers.md` | git |
| `.env.example` | да, если проект использует env | ключи с `CHANGE_ME`-значениями, без секретов | Prep, Deploy |
| `frontend/` из темплейта компании | да, если у проекта есть UI | копия `$SKILL_DIR/assets/frontend-next-14-starter/` | Design (UI), Prep |
| Локальный git-репозиторий + первый коммит | да | git | аудит, Prep, Deploy |

Все выходные артефакты коммитятся в git по завершении работы скилла.
Коммит-мессадж: что создано и на основании какого входа (запрос
пользователя → `intent.md` → каркас).

## 4. Guidance — рекомендации (можно проигнорировать без тех. последствий)

Дефолты компании из `project_setup_best_practices.md`. Отклонение — только
явным решением пользователя с записью в `NOTES.md`.


- **Фронтенд — React из темплейта компании**
  `$SKILL_DIR/assets/frontend-next-14-starter/` (Next.js 14
  pages router + React 18, Tailwind 4, zustand, recharts, готовый
  Dockerfile). Не начинать фронт с нуля и не менять фреймворк. Структуру
  папок темплейта сохранять (`components/`, `hooks/`, `contexts/`,
  `utils/`, `constants/`, `styles/`), версии базового стека «заодно» не
  поднимать. Если проект идёт по дизайн-конвенциям NeuroLab — приоритет у
  скилла `/nlab:design-ui`.
- **Агенты — на pydantic-ai.** Документацию брать из
  <https://ai.pydantic.dev/llms.txt> и ходить по нужным страницам, а не
  писать API по памяти. Схемы входа/выхода — pydantic-модели, инструменты —
  типизированные tools.
- **Модели в Claude Code.** Проектирование, декомпозиция и ревью — основная
  сессия на Fable-5/Opus-5. Задачи кода — субагенты на Sonnet-5. Основная
  сессия проверяет их результат сама (диффы, тесты, линтер) и доводит
  правками; проектные решения субагентам не делегируются. Декомпозиция на
  субагентов идёт по `feature-list.json`: одна подзадача — один субагент с
  контрактом вход/выход.
- **Remix-first, write-second.** Перед тем как писать новую спецификацию,
  скилл или паттерн — проверить реестр: возможно, нужное уже есть.

### Уровень подачи — спросить в самом начале

Первый вопрос в проекте, до Discovery по существу: **«вы разработчик или
работаете с ИИ, но код не пишете?»** Прямо, без попыток угадать по тому, как
человек формулирует. Ответ записывается в проектный `AGENTS.md` разделом
«Уровень подачи» и действует на все этапы и на всех агентов — не только на
этот скилл.

| Ответ | Как отвечать в терминале |
|---|---|
| **Разработчик** | Термины без расшифровки. Можно показывать пути, диффы, команды, обсуждать компромиссы стека и архитектуры. Вопросы — технические и по существу |
| **Не-инженер** | Коротко и простыми словами. Без путей к файлам, диффов и команд в основном тексте — они уходят в конец или опускаются. Термин, которого не избежать, объясняется одной фразой при первом употреблении |

Если пользователь не ответил или ответил уклончиво — считать, что не-инженер:
лишнее объяснение разработчику стоит секунды, недостающее не-инженеру стоит
непонятого решения.

### Формат ответа для не-инженера

Каждый содержательный ответ строится по четырём пунктам, в этом порядке:

1. **Что приняли** — какое решение зафиксировано и что оно означает на
   практике.
2. **Как это будет работать** — одной-двумя фразами, на языке пользователя.
3. **Что дальше** — ровно один следующий шаг, не список возможностей.
4. **Что нужно от вас и почему** — вопросы и блокеры, каждый с причиной,
   зачем это нужно. Если ничего не нужно — так и написать.

Списка файлов, путей и команд в этих четырёх пунктах быть не должно. Если
их всё же надо показать — отдельным блоком в самом конце, с пометкой, что
это техническая часть и её можно не читать.

### Как разговаривать с пользователем

Считай по умолчанию, что перед тобой **не инженер**. Процесс от этого не
упрощается, но объясняется. Это правило действует на всю сессию, а не
только на реплики самого скилла: если работу ведёт основная сессия с
субагентами, отчитывается перед человеком именно она, и отчитывается так же.

- **Сначала смысл, потом детали.** Не начинай ответ со списка путей,
  диффов и команд. Первая фраза — что сделано и что это даёт человеку.
  Технические детали ниже, для тех, кому надо.
- **Термин при первом употреблении — одна поясняющая фраза или замена
  бытовым словом.** «Спецификация — документ, где записано, что именно
  система должна делать; по нему потом пишется код». Не «артефакт этапа Design».
  Так же с DDD, pydantic, bounded context, healthcheck, компоузом.
- **Каждый шаг закрывай тремя вещами**: что сейчас сделано, зачем это
  нужно, что дальше. Если чего-то ждёшь от человека — четвёртым пунктом,
  отдельно и конкретно.
- **Блокер — это действие человека, а не состояние системы.** Не
  «репозиторий не выдан, push отложен», а «напишите @sanchezgl в телеграм,
  попросите репозиторий под проект и передайте свой ник на GitHub — без
  этого код останется только у вас на компьютере».
- **Не проси решать то, что решить должен ты.** Вопрос пользователю
  уместен, когда от ответа зависит результат (что считать успехом, что не
  входит в задачу, как называется проект). Выбор библиотеки, раскладки
  папок и формата логов — твоя работа, а не его.
- **Вопросы — по одному смыслу за раз**, с вариантами и рекомендацией.
  «Как поймём, что работает?» лучше, чем «сформулируйте критерии приёмки в
  измеримом виде».
- **В конце — короткое резюме и ровно один следующий шаг.** Не список из
  восьми возможностей: человек не знает, какая из них важная.
- **Не выдавай незаконченное за готовое.** Если раздел спецификации
  помечен `TODO (этап Design)` — так и скажи: «это спроектируем на
  следующем шаге», а не «спецификация готова».

## 5. Enforcement — обязательные правила (нарушение = блокировка)

| Правило | Технический механизм принуждения | Когда |
|---|---|---|
| Агент не создаёт git-репозиторий сам | `gh repo create`, `git remote add` на произвольный URL и «временные» ремоуты запрещены. `git remote -v` должен вернуть URL, зафиксированный в `NOTES.md` как выданный. Ремоут отсутствует или не совпадает → push не выполняется, работа продолжается локально | перед первым push |
| Секреты и ключи не попадают в git | `.gitignore` с `.env`, `.env.*` (кроме `*.example`), `servers.md` — до первого коммита. Плюс скан индекса (проверка **A** ниже): совпадение → коммит не делается, пока значение не вынесено в env | перед каждым коммитом |
| LLM вызывается только через OpenAI-совместимый API, не через CLI | Запуск `claude` и других CLI-агентов как subprocess из кода продукта запрещён. Скан исходников (проверка **B**): совпадение → передача на следующий этап блокируется до устранения | перед handoff |
| `BASE_URL` и ключ LLM — только из env | В коде не должно быть литералов `https://openrouter.ai/api/v1` и `https://ai.nlabstudio.ru/v1` — только `os.getenv("LLM_BASE_URL")`. Скан (проверка **C**): совпадения допустимы лишь в `.env.example` и документации | перед handoff |
| Аудит append-only | `NOTES.md` и `EVIDENCE.md` только дополняются. Проверка **D**: число удалённых строк должно быть `0`, иначе коммит не делается | перед каждым коммитом |
| Нет проверяемого критерия приёмки — нет перехода дальше | Раздел «Критерии приёмки» в `intent.md` не может быть пустым или состоять из неизмеримых формулировок («работает хорошо»). Пустой/неизмеримый раздел → скилл останавливается и не отдаёт handoff | перед handoff |
| Форматы, от которых зависят следующие этапы | Проверка **H0**: `feature-list.json` — массив верхнего уровня; критерии в `intent.md`, **если уже есть**, — нумерованным списком. Полноту критериев проверяет `/nlab:prototype` (проверка P), здесь достаточно формата | перед handoff |
| Наследие темплейта не уезжает в проект | Проверка **T**: в скопированном фронтенде нет чужих доменов, счётчиков аналитики, IP-адресов чьих-то машин и секретов в клиентском бандле. Темплейт приходит из другого проекта — его следы молча уводят запросы и утекают данные | сразу после копирования темплейта |
| Discovery не пропускается по просьбе пользователя | Просьба «просто напиши код» не отменяет `intent.md`. Минимально допустимый объём — одна фраза-гипотеза + один критерий успеха; ноль — недопустим. Отклонение фиксируется в `NOTES.md` | всегда |
| Проект уносит с собой ритуал проверки скиллов | В корне есть `CLAUDE.md` со штампом `nlab-rules` и блоком «Проверка актуальности скиллов»: `test -f CLAUDE.md && grep -q 'nlab-skills-checked' CLAUDE.md` → успех. Без него правило «сверяйся с реестром» остаётся намерением и не выполняется никогда | перед handoff |

Команды проверок — выполнять как есть, результат каждой записывать в
`EVIDENCE.md` (п.8):

```bash
# A. Секреты в индексе. Ожидание: пусто. Есть вывод → не коммитить.
git diff --cached | grep -nE '(sk-[A-Za-z0-9]{16,}|gh[pous]_[A-Za-z0-9]{20,}|(API_KEY|TOKEN|SECRET|PASSWORD)=[^C[:space:]][^[:space:]]{7,})'

# B. Вызов LLM через CLI из кода продукта. Ожидание: пусто.
grep -rnE -A2 '(subprocess|Popen|os\.system|child_process|execSync|spawn)' \
  --include='*.py' --include='*.js' --include='*.ts' . \
  | grep -iE 'claude|codex|gemini|copilot'

# C. Захардкоженный BASE_URL. Ожидание: пусто (в .env.example — допустимо).
grep -rn 'openrouter.ai\|ai.nlabstudio.ru' \
  --include='*.py' --include='*.js' --include='*.ts' .

# D. Append-only аудит. Формат вывода: <добавлено> <удалено> <файл>.
# Ожидание: вторая колонка = 0 для обоих файлов.
git diff --cached --numstat -- project-docs/NOTES.md project-docs/EVIDENCE.md

```

```bash
# H0. Форматы, от которых зависят проверки на Design. Ожидание: OK.
python3 - <<'PY'
import json, re, pathlib
errs = []
data = json.loads(pathlib.Path("project-docs/feature-list.json").read_text())
if not isinstance(data, list):
    errs.append(f"feature-list.json — {type(data).__name__}, нужен массив верхнего уровня")
else:
    for i, f in enumerate(data):
        if not isinstance(f, dict) or "id" not in f:
            errs.append(f"элемент {i} не объект или без поля id")
t = pathlib.Path("project-docs/intent.md").read_text()
b = re.search(r"##\s*Критерии приёмки\s*\n(.*?)(?=\n##\s|\Z)", t, re.S)
# на Discovery критериев может ещё не быть — их снимает /nlab:prototype;
# ругаемся только если раздел заполнен, но не нумерованным списком
if b and b.group(1).strip() and not re.findall(r"^\s*\d+\.", b.group(1), re.M):
    errs.append("критерии есть, но не нумерованным списком — следующие этапы их не прочитают")
print("\n".join(errs) if errs else "OK: форматы верные")
PY

# T. Наследие темплейта. Ожидание: OK. Запускать сразу после копирования фронта.
python3 - <<'PY'
import pathlib, re
root = pathlib.Path("frontend")
if not root.exists():
    print("OK: фронтенда нет"); raise SystemExit(0)
PATTERNS = [
    (r"https?://(?!localhost|127\.0\.0\.1)[\w.-]+\.[a-z]{2,}", "чужой домен"),
    (r"\b(?:10|172|192)\.\d{1,3}\.\d{1,3}\.\d{1,3}\b",          "IP чьей-то машины"),
    (r"mc\.yandex\.ru|googletagmanager|google-analytics|gtag\(", "чужой счётчик аналитики"),
    (r"\bym\(\s*\d+",                                            "идентификатор Метрики"),
]
SECRET_IN_BUNDLE = r"env:\s*\{[^}]*\b(?:SMTP_PASS|.*_SECRET|.*_KEY|PASSWORD)\b"
hits = []
for f in root.rglob("*"):
    if not f.is_file() or f.suffix not in {".js", ".jsx", ".ts", ".tsx", ".mjs", ".json"}:
        continue
    if "node_modules" in f.parts or f.name == "package-lock.json":
        continue
    text = f.read_text(errors="ignore")
    # комментарии убираем, но `//` в схеме URL — не комментарий:
    # наивный r"//[^\n]*" съедает https://app.example.com и прячет улику
    body = re.sub(r"(?<!:)//[^\n]*|/\*.*?\*/", "", text, flags=re.S)
    for pat, label in PATTERNS:
        for m in re.findall(pat, body):
            if "vercel.com" in m or "nextjs.org" in m or "github.com" in m:
                continue
            hits.append(f"{f}: {label} — {m}")
    if f.name == "next.config.js" and re.search(SECRET_IN_BUNDLE, body, re.S | re.I):
        hits.append(f"{f}: секрет в блоке env — Next.js инлайнит его в браузерный бандл")
print("\n".join(sorted(set(hits))) if hits else "OK: следов чужого проекта нет")
PY
```

`grep` возвращает код 1, когда совпадений нет — для проверок A–C это и есть
успех. Не трактуй ненулевой код возврата как сбой проверки.

Если непонятно, guidance это или enforcement — считать enforcement и
остановиться до подтверждения человека.

## 6. Процесс (шаги)

1. **Прочитать правила с диска**: `$SKILL_DIR/assets/AGENTS.md` и
   `$SKILL_DIR/references/project_setup_best_practices.md`. Тут же **выполнить
   проверку актуальности скиллов** командой из `$SKILL_DIR/assets/CLAUDE.md`
   (п.10) и сказать пользователю результат: стартовать новый проект на
   устаревшем пакете — значит зашить в него отменённые правила.
2. **Спросить уровень подачи** (п.4): разработчик или не-инженер. Это первый
   вопрос в проекте — от него зависит форма всех дальнейших ответов.
3. **Понять, что за проект.** Задать пользователю ровно то, чего не хватает
   для `intent.md`: что должно получиться, как проверить, что НЕ входит, и
   **как проект называется**. Имя нужно именно здесь: по нему выдают
   репозиторий и от него идут имена модулей, а рабочее имя папки
   (`TestAgent`, `new-project`) для этого не годится. Не задавать вопросов о
   деталях, которые разрешено решать самому (п.7).
4. **Определить тип проекта** — сервис / агент или мультиагентная система /
   фронтенд / комбинация. От этого зависят шаги 7–9 и состав стека.
5. **Проверить доступы**: выдан ли git-репозиторий (URL); нужен ли проекту
   LLM и есть ли ключ (OpenRouter или гейт nlab). Чего нет — не изобретать
   обход, а зафиксировать как блокер в `intent.md` и сказать пользователю,
   что и у кого запросить (п.2).
6. **Написать `intent.md`**: гипотеза результата, измеримые критерии
   приёмки, границы задачи, предположения (помеченные как предположения),
   блокеры из шага 4. Пустых разделов быть не должно.
7. **Развернуть каркас**: `AGENTS.md` в корень (правила реестра + проектная
   специфика: домен, стек, ограничения) — при копировании **заполнить штамп
   `nlab-rules`** своей версией плагина и датой, чтобы потом было видно, из
   какой версии сделана копия; `CLAUDE.md` в корень из
   `$SKILL_DIR/assets/CLAUDE.md` — тот же штамп, подставить имя проекта и
   одну строку о нём из `intent.md`, остальное (отсылку к `AGENTS.md` и блок
   проверки скиллов) оставить как есть; `NOTES.md` (первая запись — дата,
   этап Discovery, принятые решения и почему), `feature-list.json`
   (декомпозиция критериев приёмки в подзадачи с `acceptance` у каждой),
   `EVIDENCE.md` (первая запись — что проверено на этом шаге), `.gitignore`,
   `.env.example` при необходимости.
8. **Зафиксировать стек** по п.4 и записать выбор в `NOTES.md` с
   обоснованием. Любое отклонение от дефолта компании — отдельной строкой с
   пометкой, что это решение пользователя.
9. **Развернуть фронт**, если у проекта есть UI: скопировать
   `$SKILL_DIR/assets/frontend-next-14-starter/` в
   `<проект>/frontend`, заменить `name` в `package.json` на имя проекта,
   выполнить `npm install` и убедиться, что `npm run dev` поднимается.
9a. **Прогнать проверку T по скопированному фронтенду** — сразу, не
   откладывая: темплейт приходит из другого проекта, и его наследие
   (чужой домен в конфиге, счётчик аналитики, IP чьей-то машины) уводит
   запросы и утекает данные молча. Найденное чинить здесь же, а не когда
   пользователь упрётся в неработающий вход.
10. **Собрать `project-docs/PLAN.html`** по шаблону
    `$SKILL_DIR/assets/plan.template.html` — из `intent.md` и
    `feature-list.json`: суть, что система делает, как работает по шагам, что
    на выходе, критерии, что не входит, задачи, открытые вопросы, блокеры.
    Раздел «Как устроено внутри» на этом этапе опустить — его заполнит
    `/nlab:code-design`. Язык простой **всегда**, независимо от уровня
    подачи: этот файл нужен именно чтобы объяснить проект человеку. Руками
    не писать и сгенерированное не править — только пересобирать.
11. **Прогнать enforcement-проверки** из п.5 (A–D и H0: секрет-скан, скан
    CLI-вызовов LLM, захардкоженный `BASE_URL`, append-only, форматы
    `intent.md` и `feature-list.json`) и записать результат каждой в `EVIDENCE.md`
    проверяемой формулировкой.
12. **`git init` (если репозитория ещё нет) и первый коммит.** Push — только
    в выданный репозиторий и только после проверки `git remote -v`. Нет
    выданного репо → подать заявку `onboard <проект>` (best practices п.5),
    до ответа коммит локальный, push отложен, причина записана в `NOTES.md`.
13. **Отдать handoff** (п.9) по формату из п.4 — для не-инженера строго
    четырьмя пунктами (что приняли / как будет работать / что дальше / что
    нужно от вас), и обязательно сказать, что открыть `project-docs/PLAN.html`
    двойным щелчком — там всё то же самое, но подробно:
    что теперь есть у проекта и что это даёт, что осталось спроектировать
    на следующем шаге, какие блокеры и что по каждому сделать человеку,
    и один конкретный следующий шаг. Список файлов — после этого, а не
    вместо этого.

## 7. Уровень автономии и обоснование

`autonomy_level: R4` для основной части — Discovery и локальные файлы
полностью обратимы. По шагам уровень разный:

| Шаг | Обратимость | Уровень | Что это значит |
|---|---|---|---|
| Вопросы, `intent.md`, каркас, `feature-list.json` | полностью обратимо | R4 | действовать самостоятельно, не переспрашивать по мелочам |
| Копирование темплейта фронта, `npm install` | обратимо (локально) | R4 | самостоятельно |
| Выбор стека с отклонением от дефолта компании | обратимо, но влияет на весь проект | R2 | только по явному решению пользователя + запись в `NOTES.md` |
| Первый push в выданный репозиторий | труднообратимо (общая история) | R2 | подтверждение remote по `git remote -v` + согласие пользователя |
| Создание git-репозитория | необратимо в части владения/доступов | **R0 — запрещено** | делает только человек через ответственных |

Правило по умолчанию: сомневаешься в обратимости — считай, что действие
необратимо, и спроси.

## 8. Логирование и аудит

В `EVIDENCE.md` по завершении скилла должны быть проверяемые записи, а не
оценки. Обязательный минимум:

- `intent.md` создан; критериев приёмки — N, из них измеримых — N (перечислить);
- каркас: какие файлы созданы (список путей), какие уже существовали и не тронуты;
- секрет-скан индекса выполнен, команда и результат («0 совпадений» или что найдено и куда вынесено);
- скан CLI-вызовов LLM выполнен, команда и результат;
- проверка append-only (`git diff --cached --numstat -- project-docs/NOTES.md project-docs/EVIDENCE.md`) — удалённых строк 0;
- проверка **H0** — критерии в `intent.md` нумерованным списком: найдено N;
  `feature-list.json` — массив, элементов N, все с `id`;
- штамп `nlab-rules` в `AGENTS.md` и `CLAUDE.md` проставлен: версия, дата;
- `CLAUDE.md` в корне создан, блок проверки скиллов на месте
  (`grep -q 'nlab-skills-checked' CLAUDE.md` — найдено);
- проверка **T** — следов чужого проекта во фронтенде: 0 (или перечислить, что вычищено);
- уровень подачи зафиксирован в `AGENTS.md` (разработчик / не-инженер);
- `PLAN.html` собран из `intent.md` и `feature-list.json`, коммит источника указан;
- фронт (если разворачивался): `npm install` — код возврата, `npm run dev` — поднялся на порту N;
- доступы: репозиторий выдан (URL) / не выдан — блокер; ключ LLM есть / запрошен у ответственных;
- проверка версии скиллов в реестре: команда выполнена, установленная версия
  и версия реестра выписаны (или «реестр недоступен» и почему).

В `NOTES.md` — решения и их обоснование: тип проекта, выбранный стек,
каждое отклонение от дефолта компании с пометкой «решение пользователя»,
URL выданного репозитория при получении.

## 9. Передача следующему скиллу (Handoff)

Передаётся скиллу **`/nlab:prototype`** — он собирает макет и снимает на нём
детальные требования. Если у проекта нет интерфейса вообще (библиотека,
фоновый обработчик), макет пропускается и работа идёт сразу на
`/nlab:code-design`.

Обязательно на выходе: `intent.md` с гипотезой результата и границами,
`AGENTS.md` в корне со штампом версии и уровнем подачи, инициализированные
`NOTES.md` / `EVIDENCE.md` / `feature-list.json`, `PLAN.html`, всё
закоммичено. **Детальных критериев приёмки здесь ещё нет** — это нормально,
их снимает `/nlab:prototype` на макете.

Скиллы не переключаются сами: закончив, скажи пользователю, что следующий
шаг — `/nlab:prototype`, и предложи перейти к нему сразу.

Дальше по цепочке, в зависимости от проекта:

| Что дальше | Кто делает |
|---|---|
| Макет интерфейса и снятие требований на нём | скилл `/nlab:prototype` — **следующий шаг по умолчанию** |
| Архитектура по DDD, схема агентов, модели данных | скилл `/nlab:code-design` |
| UI по дизайн-системе NeuroLab | скилл `/nlab:design-ui` |
| Подготовка репозитория к деплою | скилл `/nlab:dokploy-prep` |
| Деплой | скилл `/nlab:dokploy` |

Если остались блокеры из п.6 шага 4 (нет репозитория, нет ключа LLM) —
передавать дальше можно, но блокер обязан быть виден в `intent.md` и
озвучен пользователю; Deploy с незакрытым блокером не стартует.

## 10. Авто-обновление

`update_check: per_session` — один раз при первом использовании скилла в
сессии сверить `version` с `registry_url`.

Сверять **командой**, а не намерением: готовый блок лежит в
`$SKILL_DIR/assets/CLAUDE.md` и копируется в каждый новый проект. Он
сравнивает версию из `~/.claude/plugins/installed_plugins.json` (ключ
`nlab@nlab-vibeskills`) с версией `.claude-plugin/plugin.json` в `origin/main`
реестра (клон — `~/.claude/plugins/marketplaces/nlab-vibeskills`, `git fetch`
там только читает) и не чаще раза в сутки — штамп с датой в
`~/.claude/.nlab-skills-checked`. Формулировка без команды не работает:
именно так пользователь оказывается на версии, отставшей на пять релизов,
и делает проект по отменённым правилам.

| Разница версий | Действие |
|---|---|
| Совпадает | Работать дальше молча |
| Патч/минорная | Сообщить одной строкой и предложить `/plugin marketplace update nlab-vibeskills` |
| Мажорная | Не работать по новым правилам без подтверждения — сообщить о ломающих изменениях и спросить |
| Реестр недоступен | Работать на локальной версии, отметить в `EVIDENCE.md`: «проверка обновления не выполнена» |

Обновление плагина выполняет пользователь: Claude Code не даёт агенту
обновлять плагины самостоятельно. Это guidance-механизм — недоступность
реестра не блокирует работу скилла.

**Обратная связь в реестр.** Наткнулся на то, чего в скилле нет или что в нём
неверно — API отвечает не так, как описано, шаг не проходит, нашлась грабля,
есть что улучшить — не молчи и не правь локальную копию: заведи issue в реестре
и продолжай работу обходным путём. Сначала поищи существующее
(`gh issue list -R vibe-nlab/nlab-vibeskills -S "<ключевые слова>"`), одна
проблема — одно issue:

```bash
gh issue create -R vibe-nlab/nlab-vibeskills -l skill-feedback \
  -t "<скилл> <версия>: <суть в одну строку>" \
  -b "<что ожидал; что произошло (ошибка дословно); как воспроизвести; предложение>"
```

Без секретов и содержимого чужих проектов. Ссылку на issue запиши в `NOTES.md`
проекта и одной строкой скажи пользователю. Нет `gh` — сформулируй текст issue
пользователю, пусть заведёт сам.

## 11. Примеры

**Вход** (реплика пользователя):

> Нужен сервис, который принимает договоры в PDF и вытаскивает оттуда
> стороны, сумму и срок. Хочу веб-интерфейс, чтобы загружать файл и видеть
> результат.

**Диалог скилла** (только недостающее): как поймём, что работает — сколько
договоров и с какой точностью? Что НЕ входит — редактирование договоров,
подписание, хранение дольше сессии? Репозиторий выдан? Ключ LLM есть?

**Выход** — `intent.md`:

```markdown
# Intent: экстрактор реквизитов договора

## Гипотеза результата
Веб-сервис: пользователь загружает PDF договора и получает стороны, сумму
и срок в структурированном виде.

## Критерии приёмки
1. На контрольной выборке из 20 договоров все три поля извлечены верно
   минимум в 17 случаях (85%).
2. Ответ на договор до 20 страниц — не дольше 30 секунд.
3. PDF без текстового слоя — явная ошибка пользователю, не пустой результат.

## Границы (НЕ входит)
- редактирование и подписание договоров;
- хранение файлов дольше пользовательской сессии;
- типы документов, кроме договоров.

## Предположения
- Договоры на русском языке (подтверждено пользователем).
- Максимальный размер файла 10 МБ (предположение агента, не подтверждено).

## Блокеры
- Репозиторий не выдан — push отложен, запросить у @sanchezgl / @KirillBorovkov.
```


**Выход** — каркас: `AGENTS.md`, `NOTES.md` (решение: pydantic-ai + LLM через гейт
nlab, фронт из темплейта компании), `feature-list.json` (парсинг PDF,
извлечение полей, API, UI загрузки, контрольная выборка), `EVIDENCE.md`
(результаты проверок п.8), `.gitignore`, `.env.example` с
`LLM_BASE_URL` / `LLM_API_KEY=CHANGE_ME_...`, `frontend/` из темплейта,
локальный git-репозиторий с первым коммитом.
